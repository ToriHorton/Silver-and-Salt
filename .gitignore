node_modules/
.DS_Store
.claude/settings.json
.claude/worktrees/
Connection Hub/
_differentiation-project/

# Work-in-progress mockups (kept local, not deployed)
sketch-fonts.html
typography.html
watch-style.html

# Daily deploy self-check log (kept local, never published)
.deploy-log.json

# Private (local-only) CEO tools + data, pending a protected host.
# dashboard.html stays ignored; it is published as a copy at an obscure,
# unlinked, noindexed URL (hq-25b5a94e297e.html) per Tori's 2026-07-14 decision.
# granola-inbox.js and newsletter-data.js are published with it as its data feeds.
TASKS.md
dashboard.html
dashboard-actions.html
dashboard-newsletter.html
dashboard.css
dashboard-app.js
ecosystem.html
network/people.js
network/people-utah.js
TASK-MANAGEMENT.md
task-management.html

# Launch plan: revenue targets, funnel math, risk register. Local only.
LAUNCH-PLAN.md

# GTM plan, playbook, and fall snapshot: positioning, channels, funnel math.
# Local only. GTM-PLAN.md is canonical.
GTM-PLAN.md
GTM-PLAN.html
GTM-PLAYBOOK.md
GTM-PLAN-FALL-2026.md

# odla build output (generated by _scripts/build-site.sh)
dist/

# wrangler local state
.wrangler/
.odla/*.local.json
.odla/dev-token.json
.dev.vars
.odla/credentials.local.json
.odla/security/

# Clerk local state (sk_ lives in .env.local; never commit)
.env.local
.clerk/
.odla/handshake.local.json

# CEO dashboard goals feed (private, read by the daily agent; belongs with
# dashboard.html and granola-inbox.js, never in a deploy)
goals.js
goals.json
GTM-REVIEW-2.html
GTM-REVIEW-3.html
MEMBER-KIT-COPY.md
DESIGN-OPTIONS.html
GTM-REVIEW-4.html
PORTAL-DESIGN.html

# Internal planning and state documents (untracked 2026-08-24). GitHub Pages
# serves the repo ROOT, so anything tracked here is publicly readable
# regardless of the exclusions in _scripts/build-site.sh, which only shape
# dist/. These stay on disk and are read by agents; they are simply not
# published. Revisit once Pages is decommissioned.
MIGRATION.md
PAYMENT-SPEC.md
JOURNEYS-PLAN.md
MULTI-BRAND-PLAN.md
LEGAL-REVIEW-HOW-IT-WORKS.md
_reference/
