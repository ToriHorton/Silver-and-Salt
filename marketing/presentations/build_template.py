#!/usr/bin/env python3
"""Rust master template (default colorway). See ssc_deck.py for the toolkit."""
from ssc_deck import build_template, RUST_THEME

if __name__ == "__main__":
    build_template(RUST_THEME, "Silver-and-Salt-Capital-Template.pptx")
